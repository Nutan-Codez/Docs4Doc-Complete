<% if (user.file.length > 0) { %>
    <% user.file.forEach(function(f) { %>
        <div class="file-preview">
            <h4>
                <% if (f.description && f.description.length > 0) { %>
                    <%= f.description %>
                <% } else { %>
                    NO Description
                <% } %>
            </h4>
            <% console.log(user.file); %>
            <h4><% 
                var fileDate = new Date(f.date);
                
                // Extract date components
                var day = fileDate.getDate(); // Day of the month
                var month = fileDate.getMonth() + 1; // Month (0-based index)
                var year = fileDate.getFullYear(); // Full year
                
                // Format the date as DD-MM-YYYY or YYYY-MM-DD
                var formattedDate = `${day}-${month}-${year}`; // Change format as needed
            %>
            <%= formattedDate %></h4>
            <iframe src="/uploads/<%= f.file %>" type="application/pdf" width="100%" height="600px"></iframe>
        </div>
    <% }); %>
<% } else { %>
    <p>No files uploaded yet.</p>
<% } %>
